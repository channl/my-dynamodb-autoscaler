{
  "name": "my-dynamodb-autoscaler",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "lambda-middleware": "^0.1.0",
    "lambda-scripts": "0.1.2"
  },
  "scripts": {
    "start": "lambda-scripts start",
    "build": "lambda-scripts build",
    "test": "lambda-scripts test --env=jsdom",
    "eject": "lambda-scripts eject"
  }
}
